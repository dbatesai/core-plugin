# Release premise audit — candidate `172ca2051749118cd4cc2f4296be9d692dbb178c`

Date: 2026-07-15 (pre-flight run by Keel ahead of David's /cut-release)
Rule (check 14, from the v3.11 miss): every release-note claim names the check that would FAIL if the claim were false. Absences audited against the tree, not the diff.

## Claim → falsifying evidence

| # | Claim | Check that fails if false |
|---|---|---|
| 1 | All measured retrieval surfaces derive from a single-read capture (`captureStore`: identity, index, bodies, edges from ONE read per file) — no reader behind the snapshot id | Four barrier tests incl. the whole-harness mutation storm and the trace-with-store-deleted falsifier; suite green at the exact SHA |
| 2 | Shared-write concurrency (DC-116): `index-registry.mjs` is the sole scripted writer of `~/.core/index.json`; rename-claim lock with generation files; no stealing from live pids; verified release | Failure-injection tests at owner + operator paths; genuinely-concurrent race tests (spawn, not spawnSync) |
| 3 | Deterministic artifact identity — archive hash reproducible across invocations | Repeat-hash equality test (replaced the git-archive tar-metadata hash Hale reproduced as irreproducible); CI package-smoke |
| 4 | Evaluator is fail-closed: missing tier, unknown rung, expected∩forbidden all refused | Negative tests added at Hale's blocker round; suite |
| 5 | Shareable receipts leak no local absolute paths | Hale's attack strings kept as permanent negative tests |
| 6 | Installed-path hooks + rollback round-trip work from the packaged artifact | `packaged-install-smoke.sh` at `172ca20`: 16/16 PASS (fresh run 2026-07-15 morning) |
| 7 | Suite 926 passing; CI green at the exact SHA | `gh run list --commit 172ca20`: both CI legs completed/success (verified 2026-07-15 morning; the failing `version-check` is the pre-cut version gate, red by design) |
| 8 | ABSENCE: no functional commit rides after the PASS | `git rev-parse HEAD` == `172ca20`; no uncommitted changes to TRACKED files; in sync with origin/next (verified 2026-07-15 morning). One untracked evidence artifact exists at source root — this file — at the location `/cut-release` check 14 prescribes; `git archive` builds from the committed tree, so it cannot enter the release artifact and does not change the reviewed SHA (Hale's iteration-02 catch; wording corrected 2026-07-15) |
| 9 | ABSENCE: nothing on main missing from next | `git log next..origin/main` empty (verified) |
| 10 | Scripts run clean against a second, non-CORE store | Six-command matrix vs `local-llm-build`: all exit 1 (pass-with-warnings — corpus lint: 176 integrity WARNs, 0 FAIL; PROJECT.md under cap). No exit 2. |

Scope honesty: Hale's PASS is scoped to full-capture paths at source level. Source-level PASS and Fable 5 test evidence are NOT promoted into release-artifact or Codex-runtime proof — those are the post-cut gates (delta accept, stable pickup + rollback, independent Codex pickup), per his 2026-07-15 advisory.

## Independent review artifact (check 15)

- Hale's finite PASS verdict on `172ca20` (round 16, reaffirmed through round 22): mailbox archive `~/Documents/Projects/CORE/_mailbox/archive/` (2026-07-15 rounds), recorded in unit `obs-20260715-hale-pass-verdict-train-a-172ca20`.
- Reconfirmed in writing at the exact SHA on 2026-07-15 8:17 AM: `_mailbox/hale--memory-architecture-proof-and-immediate-feedback--2026-07-15.md` ("core-plugin is clean and frozen at 172ca205…; the scoped source PASS stands").
- Prior adversarial rounds: his 2026-07-14 HOLD verdict (four blockers, each independently reproduced) — `obs-20260714-hale-train-a-hold-verdict-four-blockers`; five superseded candidates with reproduced defects.

This was a 21-round adversarial review by a peer agent on a different model family (GPT-5.6-sol), framed independently before seeing author conclusions. Not self-graded.
