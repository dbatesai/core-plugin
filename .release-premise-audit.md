# Release premise audit — v3.14.0 (candidate `e66ad4d`)

Check 14: every release-notes claim, with the check that would FAIL if it were false.
Check 15: independent review artifacts listed at the bottom.
(Prior contents covered v3.13.1 @ `e0e1f9c`, shipped — full rewrite for this candidate.)

| Claim | Falsifying check |
|---|---|
| Every-turn evidence capture writes one row per metrics-on turn, joined by retrieval_id | `tests/scripts/turn-capture-hook.test.mjs` "metrics-on turn writes an evidence row joined..." — fails if no row or ids mismatch. Also demonstrated live: 5 real turns → 5 rows in CORE's `_metrics/turn-capture/` (demo bundle). |
| Capture is default-ON with three working off-switches | `turn-capture.test.mjs` gating tests (env off, workspace off, master off) — each fails if the switch doesn't stop the write. |
| Capture failure never blocks the turn and lands in a health counter that survives stream death | `turn-capture.test.mjs` fail-open squat test — fails if written=true on a dead stream or health lost. |
| Evidence content can never reach the exported package | `turn-capture-exporter-isolation.test.mjs` planted-canary test — fails if the canary appears in exported bytes; static test fails if `metrics-package.mjs` references the stream. |
| The evidence stream cannot leak into a git-tracked project's history | `turn-capture.test.mjs` self-gitignore test — fails if `.gitignore` absent/wrong. (Gap found LIVE during the demo on CORE, fixed `e66ad4d`.) |
| Rich-context stream and CORE_RETRIEVAL_TRACE are fully retired | `turn-capture-hook.test.mjs` retirement tests — fail if the module file exists or the hook references either. `maintenance-run.test.mjs` legacy-sweep test — fails if a leftover dir survives an apply pass. |
| retrieval-log + self-test-log rows carry producer identity; 1.0.0 rows stay valid | `turn-capture-hook.test.mjs` Link-4a tests fail without stamps; `analyze-retrieval-quality.test.mjs` current-schema suite fails if 1.0.0 rows are rejected. |
| Judge grades hit-right/noise/hindsight-miss/storage-gap and flags drift instead of guessing | `hindsight-judge.test.mjs` — one test per verdict path fails if the verdict logic breaks; drift test fails if a drifted row gets a graded verdict. |
| Judge is bounded and idempotent | `hindsight-judge.test.mjs` idempotence/limit test — fails on re-judging or unbounded batch. |
| Scorecards are append-only, chained data windows, threshold-stamped | `scorecard.test.mjs` byte-prefix append test, data_window test; `maintenance-run.test.mjs` thresholds-stamp test. |
| Tripwires fire on the five conditions with the reviewed floors and stay silent when healthy | `metrics-tripwires.test.mjs` — one test per wire fails if it doesn't trip; healthy-silence test fails if noise appears; the 20-attempt floor test fails if a short session trips the rate wire. |
| Trip messages and the default /metrics view are jargon-free | jargon-guard assertions in `metrics-tripwires.test.mjs` + `metrics-answers.test.mjs` — fail on any banned token. |
| Auto-round authoring is capped weekly and blocked by an unfinished round | `metrics-tripwires.test.mjs` staleness tests — fail if the cap or the unfinished-round guard doesn't hold; `maintenance-run.test.mjs` trigger test fails if a second same-week pass re-notes. |
| /metrics default reads pinned history only and degrades honestly | `metrics-answers.test.mjs` — not-yet-measured, capture-off, and tripped states each have a failing test; CLI test fails if `--answers` breaks. |
| The benefit row is gone everywhere | `metrics-check.test.mjs` + `render-metrics-artifact.test.mjs` absence assertions — fail if the row or section reappears. |
| Old commands keep working as shims | shim SKILL.md files exist (install-doc-guard enumerates them); the CI doc-enumeration check fails on dangling references. |
| Capture adds <25ms typical (median, 500ms hard ceiling); judge batch <10s | `metrics-perf.test.mjs` — fails if budgets are exceeded (measured ~1.4ms and 0.34s; median statistic chosen after a shared-runner disk-stall tail flaked the original p95 assertion on CI with the code unchanged). |
| Concurrent appends never tear rows | `turn-capture-chaos.test.mjs` two-process test — fails on any unparseable line or lost row (100/100 verified). |
| The whole chain works on a real project | Live demo bundle `CORE/_outputs/2026-07-24/v3140-demo/` — real capture→judge→scorecard→tripwire→display run on CORE's 531-unit store, including a genuinely blind, verifier-refused-then-frozen self-test round scoring an honest 27%. |

## Independent review artifacts (check 15)

Antigravity (Gemini 3.1 Pro), adversarial advisor with per-link Gate-B review — all in `CORE/_mailbox/`:
- `antigravity--gate-a-approved-proceed-to-build--2026-07-24.md` (spec review, 5 parameter rulings)
- `antigravity--gate-b-link-1-approved-proceed-to-link-3--2026-07-24.md` (reviewed 3 named design flags)
- `antigravity--gate-b-links-2-3-4b-5-approved--2026-07-24.md` (incl. the gap-floor intent confirmation)
- `antigravity--gate-b-component-6-approved-gate-c-ready--2026-07-24.md`
- `antigravity--gate-c-approved-at-e66ad4d-release-cleared--2026-07-24.md` (final sign-off, exact SHA, named checks)
